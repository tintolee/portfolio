generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Metric {
  id   Int      @id @default(autoincrement())
  t    DateTime
  lcp  Int
  fid  Int
  cls  Float
  @@index([t])
}
